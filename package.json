{
  "name": "baseball-analytics",
  "version": "1.0.0",
  "private": true,
  "description": "棒球 + 世界盃足球初盤分析",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "predev": "node scripts/ensure-setup.mjs",
    "dev": "concurrently -k -n backend,frontend -c blue,green \"pnpm --filter baseball-analytics-backend dev\" \"pnpm --filter baseball-analytics-frontend dev\"",
    "dev:backend": "pnpm --filter baseball-analytics-backend dev",
    "dev:frontend": "pnpm --filter baseball-analytics-frontend dev",
    "install:all": "pnpm install",
    "setup": "node scripts/ensure-setup.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=18"
  }
}
